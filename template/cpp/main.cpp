#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; ++i)
using namespace std;


int main()
{
    int n;
    cin >> n;
    vector<int> arr(n, 0);
    rep(i, n) cin >> arr[i];

    string ans = "This is a template.";
    cout << ans << endl;
    return 0;
}
